import React from "react";
import { createRoot } from "react-dom/client";
import QuizQuest from "./QuizQuest.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <QuizQuest />
  </React.StrictMode>
);
